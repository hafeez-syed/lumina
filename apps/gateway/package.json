{
  "name": "@lumina/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LUMINA software backend: the only service the browser talks to. YOU BUILD THIS.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lumina/contract": "workspace:*",
    "cors": "catalog:",
    "dotenv": "catalog:",
    "express": "catalog:",
    "pino": "catalog:",
    "pino-http": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@lumina/typescript-config": "workspace:*",
    "@types/cors": "catalog:",
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
